# 无人机路径规划程序使用手册

这份手册写给第一次接触 Python 和路径规划的人，按顺序操作即可。

## 一、程序的目标

用户先在 Excel/WPS 表格里填写基地、任务点、飞行高度、限制条件和障碍物，程序再自动寻找较短且不穿越障碍物内部的飞行路线。

- 选择 `TSP`：用一架无人机访问所有启用任务点，最后回到基地。
- 选择 `CDVRP`：用多架无人机分担任务，并检查每架无人机的容量和最大航程。

程序可以输出二维或三维任务规划结果，但不会连接或操控真实无人机。实际飞行前仍需结合当地法规、飞控系统、真实地形、天气、电池余量和安全规范重新验证。

## 二、第一次使用

需要：

- 一台联网的 Windows 电脑；
- 可以打开 `.xlsx` 文件的 Microsoft Excel 或 WPS；
- Python 3.10 或更高版本；

不需要：

- YOLO；
- MATLAB（虽然本项目是由开发者的另一套matlab代码转译）；
- 编程基础（可能还是需要）；

## 三、安装 Python

1. 打开 [Python 官方 Windows 下载页面](https://www.python.org/downloads/windows/)。
2. 下载 Python 3.10 或更高版本的 Windows 安装程序。
3. 双击安装程序。
4. 在安装窗口底部勾选 `Add Python to PATH`。
5. 选择默认安装即可。
6. 安装结束后关闭窗口。

如果以前安装过 Python，也可以直接进行下一步。

## 四、安装本项目需要的组件

1. 找到本项目文件夹。
2. 双击 `安装环境.bat`。
3. 黑色窗口会自动创建独立环境并下载组件。
4. 看到“安装完成”后，按任意键关闭窗口。

这一步通常只需做一次。以后运行程序不必重复安装。

如果下载失败：

1. 检查网络连接；
2. 关闭代理或更换网络后重试；
3. 不要删除正在使用的项目文件；
4. 仍然失败时，保留黑色窗口中的最后几行错误信息以便排查。

## 五、填写 Excel 数据

打开 `无人机路径规划数据模板.xlsx`。模板中：

- 黄色单元格允许修改；
- 灰色区域是说明或固定字段，不要修改；
- 工作表名称不要改；
- 程序只读取表格，不会把结果写回原文件。

建议第一次先复制一份模板，再在副本中修改。

### 1. “航点数据”工作表

每一行代表一个地点，列的含义如下：

| 列名 | 应该填写什么 |
|---|---|
| 编号 | 每个点唯一的整数编号 |
| 名称 | 便于自己识别的名称 |
| X或纬度 | 平面模式填 X；经纬度模式填纬度 |
| Y或经度 | 平面模式填 Y；经纬度模式填经度 |
| Z或高度 | 二维模式通常填 `0`；三维欧氏模式填 Z；经纬度三维模式填海拔（米） |
| 需求量 | 该任务点需要运送或服务的数量 |
| 是否启用 | 选择“是”表示参与规划，选择“否”表示本次忽略 |
| 备注 | 可以填写说明，不影响计算 |

基地必须满足：

- 编号固定为 `0`；
- “是否启用”必须为“是”；
- 需求量必须为 `0`；
- 所有路线都从基地出发并返回基地。

添加任务点时，在下方黄色空行继续填写即可。编号不能和已有编号重复。暂时不用的点建议把“是否启用”改成“否”，不必删除。

### 2. 怎样填写坐标

使用普通平面坐标时：

- 在“任务参数”中选择“欧氏距离”；
- 二维填写 X、Y，三维还要填写 Z；
- X、Y、Z 使用相同单位，例如全部使用米或全部使用千米；
- “距离单位”只负责结果显示，请填写与坐标一致的单位。

使用真实 GPS 坐标时：

- 在“任务参数”中选择“经纬度距离”；
- “X或纬度”填写纬度，范围是 `-90` 到 `90`；
- “Y或经度”填写经度，范围是 `-180` 到 `180`；
- 三维模式下，“Z或高度”填写海拔，单位固定为米；
- 结果距离固定按千米计算。

不要把经度和纬度填反，也不要在数字后面写“度”“米”等文字。

### 3. “任务参数”工作表

| 参数 | 说明 |
|---|---|
| 问题类型 | `TSP` 表示单机；`CDVRP` 表示无人机群 |
| 算法 | 可选 `ACO`、`GA`、`HPSO`、`SA` |
| 空间维度 | `2D` 表示二维；`3D` 表示使用航点高度并进行三维避障 |
| 距离模式 | 平面坐标选“欧氏距离”；GPS 坐标选“经纬度距离” |
| 距离单位 | 欧氏距离结果显示用的单位 |
| 最低飞行高度 | 三维路线允许使用的最低 Z 或海拔；经纬度模式按米 |
| 最高飞行高度 | 三维路线允许使用的最高 Z 或海拔；经纬度模式按米 |
| 障碍物安全距离 | 欧氏模式同坐标单位，经纬度模式按米；填 `0` 时允许贴边绕行 |
| 单机容量 | 一架无人机最多承担的需求量 |
| 单机最大航程 | 一条从基地出发并返回基地的路线最大距离 |
| 最大无人机数量 | 留空表示不限制；填写时必须是正整数 |
| 随机种子 | 相同数据和参数使用相同种子可复现结果 |

选择 `TSP` 时，容量、最大航程和无人机数量会被自动忽略。

选择 `CDVRP` 时，请特别检查：

- 每个任务点的需求量不能大于单机容量；
- 每个任务点到基地的最短往返距离不能超过单机最大航程；
- 最大无人机数量不能小到无法装下全部需求。

### 4. “障碍物区域”工作表

每一行代表一个轴对齐长方体禁区，可用于组合近似山体、建筑或禁飞空域。填写障碍物编号和名称，再填写 X、Y、Z 三个方向的最小值与最大值，并把“是否启用”设为“是”。

- 每个方向都必须满足“最小值小于最大值”；
- 欧氏模式下，障碍物与航点使用相同坐标单位；
- 经纬度模式下，X 是纬度、Y 是经度、Z 是海拔（米）；
- 有限高度的障碍物可以从上方绕过；若要形成整根不可跨越的禁飞柱，请让其 Z 范围覆盖任务参数中的全部飞行高度；
- 多个长方体可以拼接成更接近山体轮廓的区域；
- 安全距离为 `0` 时路线可以沿障碍物表面经过；实际方案建议填写正数留出余量；
- 不需要的障碍物可设为“否”，不必删除。

### 5. “算法参数”工作表

普通用户建议保留默认值。默认值与原 MATLAB 项目的主要设置一致。

- `ACO`：蚂蚁数、迭代次数、Alpha、Beta、挥发率、信息素常数；
- `GA`：种群数、TSP/CDVRP 迭代次数、代沟、交叉概率、变异概率；
- `HPSO`：粒子数、迭代次数；
- `SA`：初始温度、终止温度、链长、降温系数。

增加种群数、粒子数、链长或迭代次数，通常会延长运行时间，不保证一定得到更短路线。修改后若出现参数错误，可重新从原模板复制默认值。

## 六、保存表格

1. 按 `Ctrl+S` 保存（是的我们从这一步教起）。
2. 文件必须保持 `.xlsx` 格式。
3. 不要另存为 CSV。
4. 保存后关闭 Excel/WPS，避免文件被占用。

## 七、启动规划

1. 双击 `启动程序.bat`。
2. 在弹出的文件选择窗口中选择刚保存的 `.xlsx` 文件。
3. 程序会显示问题类型、算法、二维/三维、航点数、障碍物数、飞行高度范围、容量、航程和随机种子。
4. 仔细核对：
   - 输入 `Y` 或直接按回车：开始规划；
   - 输入 `M`：返回修改 Excel，保存并关闭后再让程序重新读取；
   - 输入 `Q`：退出，不执行规划。
5. 开始后等待“规划完成”提示。

运行时间取决于航点数量、算法和迭代次数。第一次使用建议先用模板示例或少量任务点测试。

## 八、查看结果

默认结果位于项目文件夹中的 `results` 文件夹：

### `route.png`

路线俯视图。红色大点是基地，不同颜色的折线表示不同无人机路线，障碍物显示为半透明区域。

### `route_3d.png`

三维任务才会生成。可以同时查看航点高度、绕障折线和长方体障碍物。

### `convergence.png`

收敛曲线。纵轴越低表示当前找到的总路线越短。曲线记录的是到该步为止的全局最好结果。

### `routes.csv`

可以用 Excel/WPS 打开。每一行包含：

- 无人机编号；
- 航点访问顺序；
- 该机路线距离；
- 该机载荷；
- 距离单位。

路线中的首尾编号都是 `0`，表示从基地出发并返回基地。

### `flight_path.csv`

可以用 Excel 打开，记录每架无人机实际路线上的全部转折点。三维避障产生的中间绕行点也会保存在这里。

### `solution.json`

保存完整计算结果，主要供后续 Python 程序或其他软件读取。普通用户不需要修改它。

再次运行会覆盖 `results` 中同名结果。需要保留旧结果时，请先把整个 `results` 文件夹复制并改名。

## 九、常见错误及处理

### 提示“找不到 Excel 文件”

- 确认文件没有被移动或删除；
- 重新启动并在选择窗口中选中正确文件；
- 文件扩展名必须是 `.xlsx`。

### 提示“无法读取 Excel”

- 保存并关闭 Excel/WPS；
- 确认没有修改工作表名称；
- 确认文件没有损坏；
- 用原模板重新填写数据。

### 提示“编号重复”或“基地 0 不存在”

- 检查“航点数据”的编号；
- 每个启用航点编号必须唯一；
- 编号 `0` 的基地必须存在并启用。

### 提示坐标或需求量不是数字

- 单元格中只能填写数字；
- 不要在数字后写单位；
- 不要使用破折号、问号或其他文字代替空白；
- 需求量不能为负数。

### 提示经纬度超出范围

- 纬度应在 `-90` 到 `90`；
- 经度应在 `-180` 到 `180`；
- 检查两列是否填反。

### 提示高度超出飞行范围

- 检查航点的 Z 或海拔是否位于最低和最高飞行高度之间；
- 经纬度三维模式的高度单位必须是米；
- 如果修改了飞行高度范围，也要同步检查障碍物的 Z 范围。

### 提示障碍物边界不合法

检查“障碍物区域”中每一行的三个方向，最大值必须严格大于最小值，且所有坐标都只能填写有限数字。

### 提示航点位于障碍物内或无法从基地到达

- 移动航点或调整相应障碍物边界；
- 降低安全距离，或提高允许的最高飞行高度；
- 检查多个障碍物是否意外封死了所有绕行空间。

### 提示某个航点超过容量

该点需求量大于一架无人机的容量。请提高单机容量，或把该任务拆成多个需求更小的任务点。

### 提示某个航点往返距离超过最大航程

即使只访问该点，一架无人机也无法在当前航程限制内从基地往返。请提高最大航程、调整基地或任务点位置。

### 提示“当前限制下无可行方案”

常见原因：

- 最大无人机数量太少；
- 单机容量太小；
- 单机最大航程太短；
- 多个限制组合后无法同时满足。

先把“最大无人机数量”留空，再适当提高容量或航程后重试。

### 路线图中文显示为方框

这只影响图片中的中文字体，不影响路线和数值。可以在系统中安装常见中文字体后重新运行。

## 十、给进阶用户的命令行方式

普通用户可以跳过本节。

交互运行：

```powershell
python main.py
```

直接指定参数：

```powershell
python -m uav_planner --input data.xlsx --problem cdvrp --algorithm aco --dimension 3d --distance-mode euclidean --seed 42 --output results
```

可选值：

- `--problem`：`tsp` 或 `cdvrp`；
- `--algorithm`：`aco`、`ga`、`hpso` 或 `sa`；
- `--dimension`：`2d` 或 `3d`；
- `--distance-mode`：`euclidean` 或 `haversine`；
- `--seed`：非负整数；
- `--output`：结果文件夹。

命令行中明确填写的参数优先于 Excel；没有填写的继续使用 Excel 设置。

## 十一、使用边界

支持二维&三维任务级路线规划，以及由多个轴对齐长方体组合出的静态绝对禁区。它不能处理：

- 摄像头图像和 YOLO 目标识别；
- 数字高程模型、三角网格或任意曲面地形；
- 会移动或随时间变化的动态障碍物；
- 空气动力学；
- 多机实时通信与碰撞避免；
- 飞控指令生成和真实无人机控制。

因此路线结果适合算法学习和前期方案设计，不能直接作为真实飞行安全依据。
