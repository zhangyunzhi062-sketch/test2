# 项目概述

本项目把原 MATLAB 路径规划程序迁移为 Python，实现二维航点条件下的单机 TSP 与无人机群 CDVRP。两类问题均提供蚁群算法（ACO）、遗传算法（GA）、离散混合粒子群算法（HPSO）和模拟退火算法（SA）。

# 项目目标

- 使用可编辑 Excel/WPS 模板录入航点、需求量和任务限制。
- 保留原算法类别、主要参数、路线输出和收敛过程，不要求随机结果逐点一致。
- TSP 中每个启用航点恰好访问一次并返回基地。
- CDVRP 中每条航线同时满足容量、最大航程和可选的无人机数量限制。
- 支持平面欧氏距离与真实经纬度球面距离。
- 提供中文命令行向导、结果文件、路线图、短测试和零基础使用手册。

# MATLAB 迁移结论

- 原数据以第 0 点为基地，其余点为任务点。
- TSP 使用“基地—所有任务点排列—基地”的闭环编码。
- CDVRP 在任务点排列中按容量和航程约束插入基地，将一条排列拆分为多条无人机航线。
- ACO 默认参数为 8 个蚂蚁、100 次迭代、Alpha=1、Beta=5、挥发率=0.1、信息素常数=1。
- GA 默认种群为 60，交叉率 0.9、变异率 0.05、代沟 0.9；TSP 默认 500 代，CDVRP 默认 100 代。
- HPSO 使用个体最优、全局最优的 PMX 交叉与换位变异，默认 60 个粒子、100 次迭代。
- SA 默认初温 1000、终温 0.001、链长 200、降温系数 0.9。

# 实施步骤

1. 创建并验证可编辑 Excel 模板。
2. 实现统一数据模型、距离计算和约束校验。
3. 实现 TSP/CDVRP 的四种求解器。
4. 实现中文交互入口、结果导出和绘图。
5. 执行小规模自动测试与隐私扫描。
6. 初始化 Git，提交并推送代码。
7. 编写中文 README 和零基础使用手册，再次提交并推送。
